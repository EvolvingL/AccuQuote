services:
  - type: web
    name: accuquote
    runtime: node
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
