node_modules/
# Firebase service account key — never commit
accuquote-246a3-firebase-adminsdk-*.json
*-firebase-adminsdk-*.json
# iOS secrets — never commit
iOS/AccuQuoteScan/AccuQuoteScan/Secrets.xcconfig
iOS/AccuQuoteScan/AccuQuoteScan/APIKeys.swift
.env
.env.local
.env.production
.DS_Store
*.log
.vercel
.netlify
dist/

# Xcode local user state — machine-specific, not project config
xcuserdata/
*.xcuserstate

# Local-only planning assets — not meant for repo history
*.docx
~$*
images/
IMG_*.PNG
IMG_*.png
image.jpg
AccuQuote-icon-1024.png
AccuQuote-preview.png
